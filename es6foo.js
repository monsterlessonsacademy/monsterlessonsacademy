export const getFullName = (name, surname) => {
  return name + " " + surname;
};

export const getSurname = (name, surname) => {
  return surname;
};

// export default getFullName;

// module.exports = {
//   getSurname,
//   getFullName,
// };
